#include <stdio.h>
#include "main.h"

/**
 * add - entry point
 * description - a function that add two numbers.
 * @x : first number
 * @y : second number
 * Return: Always 0 (Success)
 */

int add(int x, int y)
{
return (x + y);
}
